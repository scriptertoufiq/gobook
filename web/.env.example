# Leave empty in development — vite.config.ts proxies /api to localhost:8080,
# so the browser sees a single origin and CORS never enters the picture.
#
# Set this for a deployed build, where the frontend is served from somewhere
# the Go API is not: VITE_API_URL=https://api.example.com
VITE_API_URL=
